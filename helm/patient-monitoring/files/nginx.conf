server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /patient {
        proxy_pass http://{{ .Release.Name }}-patient-api-service:8000;
    }
}