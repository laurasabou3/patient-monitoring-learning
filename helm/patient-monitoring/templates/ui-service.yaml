apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-patient-ui-service

spec:
  selector:
    app: {{ .Release.Name }}-patient-ui

  ports:
  - port: {{ .Values.ui.service.port }}
    targetPort: {{ .Values.ui.service.targetPort }}

  type: {{ .Values.ui.service.type }}