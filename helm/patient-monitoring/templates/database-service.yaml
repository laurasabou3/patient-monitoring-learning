apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-patient-db-service

spec:
  selector:
    app: {{ .Release.Name }}-patient-db

  ports:
  - port: {{ .Values.database.service.port }}
    targetPort: {{ .Values.database.service.targetPort }}