apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ .Release.Name }}-patient-api-deployment

spec:
  replicas: {{ .Values.api.replicas }}

  selector:
    matchLabels:
      app: {{ .Release.Name }}-patient-api

  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-patient-api

    spec:
      containers:
      - name: patient-api
        image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"

        ports:
        - containerPort: {{ .Values.api.containerPort }}